// a와 b중 더 큰 수를 반환하는 함수
fun max(aG:Int, bG:Int):Int =
    if (aG>bG) aG else bG

// a와 b 중 더 작은 수를 반환하는 함수
fun min(aG:Int, bG:Int):Int =
    if (aG<bG) aG else bG

// num의 절대값을 반환하는 함수
fun abs(numG:Int):Int =
    if (numG >= 0 ) numG else -numG

